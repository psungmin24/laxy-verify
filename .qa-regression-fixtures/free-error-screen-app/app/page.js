export default function Page() {
  return (
    <main style={{ padding: 32, fontFamily: "sans-serif" }}>
      <h1>404 Not Found</h1>
      <p>This fixture intentionally renders an error-like surface even though the build still passes.</p>
      <p>Free verification should treat this as a broken app surface, not a healthy landing page.</p>
    </main>
  );
}
